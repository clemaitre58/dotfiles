- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: _vim
    ~/.vimrc: _vimrc
    ~/.bashrc: _bashrc
    ~/.gitconfig: _gitconfig

- shell:
  - [git submodule update --init --recursive, Installing submodules]
